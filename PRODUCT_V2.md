# 湾区罗盘 · 产品规划 V2

**版本**: v2.0 Draft
**日期**: 2026-07-03
**核心变更**: 从「探索游戏为主」转型为「探索 + 记忆 + 路线」三足鼎立的产品形态

---

## 一、产品定位转型

### 1.1 旧定位

> 一个以游戏探索为主的探索游戏：地图 + 文化锚点 + 剧情副本 + 印章册 + 路线辅助。

用户的核心行为路径是：看地图 → 点锚点 → 玩副本 → 集印章 → 偶尔规划路线。

### 1.2 新定位

> 一个以地图为底座的「文化探索 + 个人记忆 + 路线规划」三引擎产品。

用户的核心行为路径变为三条并列主线：

| 主线 | 一句话定位 | 情感价值 |
|------|-----------|---------|
| **探索附近** | 发现身边的文化锚点，了解大湾区的历史厚度 | 知识获得感、好奇心满足 |
| **留下记忆** | 在地图上钉下属于此刻的照片与感受，与好友分享当天所见 | 珍藏感、社交连接、时光回溯 |
| **路线规划** | 把文化锚点、记忆锚点、好友锚点串联成一条专属旅程 | 掌控感、旅程期待感 |

三者共享同一张全屏水墨地图作为主视觉底座，通过底部三个并列主按钮切换功能模式。

---

## 二、首页地图主视觉（不变 + 微调）

### 2.1 保持不变

- 全屏 3D 交互水墨地图仍是第一屏 hero。
- 文化锚点的宝石状图标、主题 overlay 染色层、东方纹样底纹遮罩、deck.gl 全球影响力弧线全部保留。
- 加载动画、移动端横屏开场云雾动画保留。

### 2.2 关键调整：底部三主按钮

当前底部仅有一个「开始探索」主按钮。改造为**三个并列主按钮**，视觉风格统一（均为 `explore-launcher` 水墨毛笔按钮风格），功能各异：

```
┌─────────────────────────────────────────────────┐
│                                                 │
│              全 屏 水 墨 地 图                   │
│        （文化锚点宝石 + 记忆锚点光点）            │
│                                                 │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│   │  探索附近  │  │  留下记忆  │  │  路线规划  │     │
│   └──────────┘  └──────────┘  └──────────┘     │
└─────────────────────────────────────────────────┘
```

- **探索附近**：沿用现有「开始探索」按钮，文案/图标不变，点击打开罗盘探索面板（附近锚点 DIY 路线 + 热门推荐路线 tab）。
- **留下记忆**：新增主按钮，图标用书法字「忆」，点击进入记忆锚点创建模式。
- **路线规划**：新增主按钮（从右上角工具栏迁移），图标用书法字「途」，点击打开路线规划面板。

### 2.3 关键调整：右上角工具栏精简

当前右上角 `tool-cluster` 包含三个按钮：语言切换、印章册、路线规划。

改造后精简为**仅保留语言/设置入口**：

- 路线规划迁移到底部三主按钮之一。
- 印章册入口移入「探索附近」面板内部（作为探索成果的附属功能，而非一级入口）。
- 右上角仅留一个齿轮/设置图标，内含：语言切换、关于、设置项。
- 当某个主功能（探索/记忆/路线）被激活后，该功能相关的次级操作按钮出现在各自的功能面板内，而非全局工具栏。

### 2.4 记忆锚点在首页地图的展示

- 用户已创建的记忆锚点以**金色光点**（`#d4a843` 外圈光晕 + `#f0c75e` 内圈实心）展示在首页地图上，与文化锚点的朱砂/水墨风格形成区分。
- 好友当天分享的记忆锚点以**淡金色光点**（降低饱和度 + 虚线外圈）展示，区分「自己的」与「好友的」。
- 首页地图默认同时展示文化锚点 + 自己的记忆锚点；好友锚点可选展示（默认开）。

---

## 三、功能一：探索附近（Explore Nearby）

### 3.1 定位

保持现有逻辑不变，是三引擎中改动最小的部分。

### 3.2 核心流程（不变）

```
点击「探索附近」
  │
  ├─ 打开罗盘探索面板（底部抽屉 / 侧边面板）
  │   ├─ Tab 1: 附近锚点 DIY 路线（基于 GPS 附近的文化锚点）
  │   └─ Tab 2: 热门推荐路线（产品预设主题路线）
  │
  ├─ 选择锚点 → 生成概览路线 → 地图绘制路线
  │
  └─ 实时导航跳转腾讯地图
```

### 3.3 印章册入口迁移

印章册从右上角工具栏迁移到探索面板内部：
- 在罗盘探索面板顶部增加一个「印章册」入口按钮。
- 副本完成后的「查看印章册」流程不变，仍自动定位到刚获得的印章。

### 3.4 文化锚点详情（不变）

- 点击文化锚点 → fly-to → 信息面板（标签 + 描述 + 图片 + 剧情副本入口）。
- 剧情副本 → 答题 → 印章奖励。

---

## 四、功能二：留下记忆（Leave a Memory）

### 4.1 定位

给用户创造一个在地图上留下记忆锚点的功能。这是本次改革的**核心新增功能**，也是产品情感价值的最大增量。

### 4.2 与现有 memory.js 的关系

项目已有一套记忆锚点的技术底座（`src/memory.js`，含 localStorage + Supabase 双模式、金色光点渲染、创建/查看/删除面板）。本次产品规划在此基础上做以下升级：

| 维度 | 现有实现 | V2 升级 |
|------|---------|--------|
| 入口 | 长按地图 / 锚点详情页底部 | 升级为底部三主按钮之一 |
| 媒体类型 | 仅 1 张照片 | 1 张照片**或** 1 段视频（二选一） |
| 语音 | 无 | 新增 1 段可选语音留言 |
| 文字 | 最多 200 字 | 不变 |
| 每日限额 | 无 | 每天 5 个 |
| 好友可见性 | 仅自己可见 | 新增好友当天可见 |
| 创建界面 | 简单表单 | 发帖式沉浸创建界面 |

### 4.3 记忆锚点数据模型（V2）

```javascript
{
  id: 'mem-1751540000000-rand',
  lat: 22.5173,
  lng: 113.9347,
  mediaType: 'photo',           // 'photo' | 'video'
  mediaUrl: 'blob:... 或 https://...',
  voiceUrl: 'blob:... 或 https://...',  // 可选，语音留言
  note: '刚来深圳的那个傍晚',     // 最多 200 字
  createdAt: '2026-07-03T15:52:00Z',
  dateKey: '2026-07-03',        // 日期标识，用于好友可见性控制
  linkedAnchorId: 'N-SC01',     // 可选，关联的官方锚点
  authorId: 'user-xxx',         // 创建者 ID
  authorName: '张三',            // 创建者显示名
}
```

### 4.4 创建流程

```
点击底部「留下记忆」主按钮
  │
  ├─ 主视觉切换：文化锚点宝石淡出，地图聚焦到用户当前位置
  │
  ├─ 弹出发帖式创建界面（底部抽屉 / 全屏模态）
  │   ├─ 媒体区（顶部大图区域）
  │   │   ├─ 拍照（调用摄像头）
  │   │   ├─ 录视频（调用摄像头，限时 15 秒）
  │   │   └─ 从相册选择（1 张图 或 1 段视频）
  │   ├─ 语音区（可选）
  │   │   ├─ 按住录音，松开停止
  │   │   └─ 最长 60 秒，支持试听与重录
  │   ├─ 文字区
  │   │   └─ <textarea maxlength="200"> 一句话描述
  │   ├─ 位置区
  │   │   └─ 自动填充当前 GPS 坐标（可在地图上微调）
  │   └─ 发布按钮
  │
  ├─ 点击「发布」
  │   ├─ 校验：当天已创建数量 < 5
  │   ├─ 媒体压缩（图片 max-width 1280px / 视频 max 720p 15s）
  │   ├─ 访客模式 → 存 localStorage（压缩为缩略图）
  │   └─ 登录模式 → 上传 Supabase Storage + 元数据写 DB
  │
  └─ 地图上出现金色光点 + 涟漪动效
```

### 4.5 发帖式创建界面设计

参考社交媒体发帖交互，但融入水墨视觉体系：

- **底板**：Porcelain `#F7F4EC` 半透明面板，毛笔标题「留下一刻」。
- **媒体上传区**：大尺寸预览框（16:9），空状态显示水墨相机图标 + 「拍照 / 相册 / 视频」三个小按钮。
- **语音条**：波形图标 + 时长显示，按住录音，松开停止。录制完显示播放条。
- **文字输入**：宣纸质感 textarea，placeholder 为「这个瞬间对你意味着什么？」。
- **位置显示**：底部小字显示当前坐标 / 关联锚点名，支持点击微调。
- **发布按钮**：与三主按钮同风格的水墨毛笔按钮，文案「钉下记忆」。

### 4.6 每日限额规则

- 每个用户每天最多创建 **5 个**记忆锚点（按 `dateKey` 计算，UTC+8 自然日）。
- 创建界面顶部显示今日剩余配额：「今日还可留下 N 个记忆」。
- 达到上限时按钮置灰，提示「今日记忆已满，明天再留下新的吧」。
- 限额目的：鼓励质量而非数量，控制存储成本。

### 4.7 好友系统与记忆分享

#### 4.7.1 好友关系

- 轻量好友系统：用户可通过手机号 / 邀请码添加好友。
- 好友列表存储在 Supabase `friendships` 表（双向确认）。
- 访客模式无好友功能，登录后可用。

#### 4.7.2 好友记忆可见性规则（核心社交机制）

```
好友今天发的记忆锚点 → 你能看到（淡金色光点 + 可点击查看详情）
好友昨天发的记忆锚点 → 你看不到（过期自动隐藏）
你的记忆锚点 → 永久对自己可见（不受时间限制）
```

- **设计意图**：制造「限时窥见」的社交压力感 —— 想看朋友今天看到了什么美景，就得今天打开看，明天就没了。这增加了日活动力和社交互动性。
- **技术实现**：查询好友记忆时，SQL 条件为 `WHERE dateKey = CURRENT_DATE AND authorId IN (好友列表)`。
- 好友记忆锚点在地图上以**淡金色虚线外圈光点**呈现，点击查看详情（照片/视频 + 文字 + 语音），但不可编辑/删除。

#### 4.7.3 好友记忆 Feed

- 在记忆创建界面内增加一个「好友今日」区域（或独立 tab）。
- 横向滚动的卡片流，展示好友今天发布的记忆锚点缩略图 + 一句话 + 头像。
- 点击卡片 → 地图 fly-to 到该锚点位置 + 打开详情。

### 4.8 记忆锚点地图渲染

| 锚点类型 | 视觉样式 | 交互 |
|---------|---------|------|
| 自己的记忆 | 金色光点：外圈 `#d4a843` 实线光晕 + 内圈 `#f0c75e` 实心 | 点击查看详情、编辑、删除 |
| 好友的记忆（当天） | 淡金色光点：外圈 `#d4a843` 虚线光晕 + 内圈 `#e8c878` 半透明 | 点击查看详情（只读） |
| 好友的记忆（过期） | 不显示 | — |

### 4.9 存储策略

| 模式 | 照片/视频存储 | 语音存储 | 元数据 | 容量 |
|------|-------------|---------|--------|------|
| 访客 | 图片压缩为 base64 缩略图存 localStorage；视频不支持（需登录） | 不支持 | localStorage | ~5MB，约 15-20 条带缩略图 |
| 登录 | 原图/视频上传 Supabase Storage `memory-photos` bucket | 语音上传 Supabase Storage `memory-voices` bucket | Supabase DB + localStorage 双写 | Supabase 免费 1GB Storage |

视频压缩策略：客户端用 MediaRecorder API 录制，max 720p / 15s / ~2MB。

---

## 五、功能三：路线规划（Route Planning）

### 5.1 定位

将现有的路线规划功能从「探索的附属」提升为独立主线，并扩展锚点来源。

### 5.2 与现有 route.js 的关系

项目已有 `src/route.js`（OSRM 道路估算 + 直线降级 + 贪心排序）和 `src/ui.js` 中的 `renderRoutePlanner`。本次升级扩展锚点来源和交互。

### 5.3 核心流程

```
点击底部「路线规划」主按钮
  │
  ├─ 打开路线规划面板（底部抽屉 / 侧边面板）
  │
  ├─ 锚点来源（四类可选，支持混合勾选）
  │   ├─ 文化锚点（64 个官方 PGC 锚点）
  │   ├─ 历史锚点（已探索的文化锚点子集）
  │   ├─ 我的记忆锚点（自己创建的记忆锚点）
  │   └─ 好友记忆锚点（好友当天分享的记忆锚点）
  │
  ├─ 路线模式
  │   ├─ 自定义路线：勾选锚点 → 贪心排序 → 生成路线
  │   └─ 推荐路线：产品预设主题路线（已有功能，一键使用）
  │
  ├─ 路线结果
  │   ├─ 地图绘制路线（道路折线 / 直线降级）
  │   ├─ 预计距离与时间
  │   └─ 导航跳转腾讯地图
  │
  └─ 路线可包含混合锚点类型
      例：文化锚点 A → 我的记忆锚点 B → 好友记忆锚点 C → 文化锚点 D
```

### 5.4 锚点类型在路线中的视觉区分

路线上的站点标记按类型用不同样式：

| 站点类型 | 标记样式 |
|---------|---------|
| 文化锚点 | 朱砂宝石图标（现有样式） |
| 我的记忆锚点 | 金色光点 |
| 好友记忆锚点 | 淡金色虚线光点 |

路线连线统一为 Jade Cyan `#3F7D6E` 流动虚线动画。

### 5.5 推荐路线（已有功能保留）

产品预设的主题路线不变，如「海上丝路」「科技走廊」「岭南文脉」等。用户一键使用后，路线绘制在地图上。

### 5.6 路线中记忆锚点的特殊性

- 好友记忆锚点只能加入当天路线（明天该锚点过期后，路线中该站点标记为「已过期」灰色状态）。
- 路线规划面板中，好友锚点列表标注「今日可见」标签，提示时效性。

---

## 六、UI 架构调整总览

### 6.1 底部三主按钮

```
HTML 结构:
<div id="main-actions" class="main-actions">
  <button class="explore-launcher" id="explore-btn">
    <span class="explore-ico">探</span>
    <span>探索附近</span>
  </button>
  <button class="explore-launcher" id="memory-btn">
    <span class="explore-ico">忆</span>
    <span>留下记忆</span>
  </button>
  <button class="explore-launcher" id="route-btn">
    <span class="explore-ico">途</span>
    <span>路线规划</span>
  </button>
</div>
```

- 三个按钮等宽，风格统一为现有 `explore-launcher` 水墨毛笔按钮。
- 当前激活的按钮高亮（朱砂底色），其余按钮为默认水墨底色。
- 移动端横屏：三按钮居中排列，宽度 `min(30vw, 160px)`。
- PC 端：三按钮居中排列，宽度 `160px`。

### 6.2 右上角工具栏精简

```
当前: [语言切换] [印章册] [路线规划]
改造后: [设置 ⚙]
         └─ 语言切换
         └─ 关于
         └─ 设置项
```

### 6.3 功能模式切换

点击不同主按钮时，地图上的 UI 状态切换：

| 模式 | 文化锚点 | 记忆锚点 | 好友锚点 | 面板内容 |
|------|---------|---------|---------|---------|
| 探索附近 | 正常显示 | 显示 | 可选显示 | 附近锚点 DIY 路线 + 推荐路线 |
| 留下记忆 | 淡出 | 高亮 | 高亮 | 发帖式创建界面 + 好友今日 Feed |
| 路线规划 | 正常显示 | 显示 | 显示（标注今日） | 四类锚点勾选 + 路线生成 |

---

## 七、数据库设计扩展

### 7.1 新增表

```sql
-- 好友关系表
CREATE TABLE friendships (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  requester_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  addressee_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  status TEXT NOT NULL DEFAULT 'pending', -- 'pending' | 'accepted' | 'blocked'
  created_at TIMESTAMPTZ DEFAULT NOW(),
  accepted_at TIMESTAMPTZ,
  UNIQUE(requester_id, addressee_id)
);

-- 语音存储（memory_anchors 表扩展字段）
ALTER TABLE memory_anchors ADD COLUMN media_type TEXT DEFAULT 'photo';
ALTER TABLE memory_anchors ADD COLUMN voice_url TEXT;
ALTER TABLE memory_anchors ADD COLUMN date_key TEXT NOT NULL;
ALTER TABLE memory_anchors ADD COLUMN author_name TEXT;
```

### 7.2 好友记忆可见性 RLS 策略

```sql
-- 用户可以看到自己和好友当天发布的记忆
CREATE POLICY "Users can view own and friends' today memories" ON memory_anchors
  FOR SELECT USING (
    auth.uid() = user_id  -- 自己的记忆，永久可见
    OR (
      -- 好友的记忆，仅当天可见
      author_id IN (
        SELECT requester_id FROM friendships WHERE addressee_id = auth.uid() AND status = 'accepted'
        UNION
        SELECT addressee_id FROM friendships WHERE requester_id = auth.uid() AND status = 'accepted'
      )
      AND date_key = to_char(NOW() AT TIME ZONE 'Asia/Shanghai', 'YYYY-MM-DD')
    )
  );
```

---

## 八、i18n 文案 Key 规划（新增）

```
action.explore → 探索附近
action.memory → 留下记忆
action.route → 路线规划
action.settings → 设置

memory.create_title → 留下一刻
memory.media_photo → 拍照
memory.media_video → 视频
memory.media_album → 相册
memory.voice_hint → 按住录音
memory.voice_release → 松开停止
memory.remaining_today → 今日还可留下 {n} 个记忆
memory.limit_reached → 今日记忆已满，明天再留下新的吧
memory.publish → 钉下记忆
memory.friends_today → 好友今日
memory.friend_anchor → {name} 的记忆

friend.add → 添加好友
friend.search → 搜索手机号
friend.invite → 邀请好友
friend.list → 好友列表
friend.request_sent → 邀请已发送
friend.request_accepted → 已成为好友

route.anchor_cultural → 文化锚点
route.anchor_memory_mine → 我的记忆
route.anchor_memory_friend → 好友记忆
route.anchor_expired → 已过期
route.custom → 自定义路线
route.recommended → 推荐路线
```

---

## 九、实施阶段建议

### Phase 1 — 三主按钮 UI 重构（1-2 天）

- 改造 `renderToolCluster`：精简为设置入口，迁移印章册/路线按钮。
- 新增底部三主按钮 `#main-actions`，复用 `explore-launcher` 样式。
- 功能模式切换状态机：点击不同按钮切换地图 UI 状态。
- 印章册入口迁移到探索面板内部。

### Phase 2 — 留下记忆功能升级（3-5 天）

- 创建界面升级为发帖式：媒体区（拍照/录像/相册）+ 语音录制 + 文字 + 位置。
- 每日限额逻辑（5 个/天）。
- `memory_anchors` 数据模型扩展：`mediaType` / `voiceUrl` / `dateKey` / `authorName`。
- Supabase Storage 新增 `memory-voices` bucket。
- 媒体压缩策略升级（图片不变，视频用 MediaRecorder API）。

### Phase 3 — 好友系统（3-5 天）

- Supabase `friendships` 表 + RLS 策略。
- 好友添加流程（手机号搜索 / 邀请码）。
- 好友记忆可见性逻辑：当天可见，过期隐藏。
- 好友记忆 Feed 面板。
- 好友锚点地图渲染（淡金色虚线光点）。

### Phase 4 — 路线规划升级（2-3 天）

- 路线规划面板扩展锚点来源：四类 checkbox（文化/历史/我的记忆/好友记忆）。
- 混合锚点路线生成与渲染。
- 好友锚点时效性标注。
- 推荐路线功能保持不变。

### Phase 5 — 集成测试与打磨（2-3 天）

- 三模式切换流畅性。
- 移动端横屏全流程验证。
- 六语言文案翻译。
- 存储容量与降级策略测试。
- 视觉一致性走查（DESIGN.md 规范）。

---

## 十、风险与降级

| 风险 | 降级方案 |
|------|---------|
| 视频上传耗流量/存储 | 访客模式不支持视频，仅支持照片；登录模式视频限制 720p/15s |
| Supabase 未配置 | 好友功能不可用，记忆锚点降级为纯本地模式 |
| 好友系统网络不可达 | 好友 Feed 显示「暂时无法加载好友动态」，自己的记忆功能不受影响 |
| localStorage 容量满 | 提示「本地存储已满，请登录后同步到云端」 |
| 每日限额被绕过（访客模式） | 访客模式按设备 ID + dateKey 在 localStorage 计数，清缓存可绕过但无实际危害 |
| 好友记忆过期体验突兀 | 过期锚点在路线中标记「已过期」灰色，不直接消失 |

---

## 十一、验收标准

- 底部三主按钮在 PC 端和移动端横屏均正常展示，点击切换功能模式。
- 右上角仅保留设置入口，语言切换在设置内。
- 「留下记忆」创建界面支持拍照/录像/相册 + 语音 + 文字，每日限额 5 个正常生效。
- 登录用户可添加好友，可看到好友当天发布的记忆锚点，明天自动隐藏。
- 好友记忆锚点在地图上以淡金色虚线光点呈现，与自己的金色光点有视觉区分。
- 「路线规划」面板支持四类锚点来源勾选，可生成混合锚点路线。
- 文化锚点宝石在「留下记忆」模式下淡出，在其他模式下正常显示。
- 所有新增 UI 文案支持六语言。
- 所有动效支持 `prefers-reduced-motion`。
- 移动端横屏 390px / PC 端 1440px 均正常显示。
